name: Refresh notes
on:
  push:
    branches:
    - master
    paths:
    - modules/*
    - input/*/*
    - lecture-notes/*/*
    - .github/workflows/*
    - dev/*
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
      with:
        path: repo
    - name: Setup Node
      uses: actions/setup-node@v1
      with:
        node-version: 10
    - name: Setup Python
      uses: actions/setup-python@v1
      with:
        python-version: '3.x'
    - name: Install markdown-include
      run: npm install markdown-include
    - name: Install grip
      run: pip install grip
    - name: Expand MarkDown files
      run: ./repo/dev/expand.sh
    - name: Push Markdown files
      run: ./repo/dev/pages.sh
    - name: Generate and push HTML files
      run: ./repo/dev/html.sh
