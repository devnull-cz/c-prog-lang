int
main(void)
{
	/*
	 * See what "echo $?" prints out in the shell.
	 *
	 * Then also try "return (-1)" and "return (1000)" and check $? again.
	 * You will NOT get -1 and 1000.  See the lecture notes for explanation.
	 */
	return (13);
}
